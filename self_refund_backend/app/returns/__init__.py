"""
Returns domain.

    rules.py       eligibility, quantity accounting, weight check (pure rules)
    states.py      legal status transitions
    policy.py      return policy values (window, retries, photo requirement)
    repository.py  database queries for returns
    service.py     submit a customer return
    review.py      staff decisions: approve, reject, mark refunded
"""
